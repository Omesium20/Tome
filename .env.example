# Read by `docker compose` (via -f Dockercompose.yaml) for variable substitution in
# that file — copy to `.env` and adjust. Separate from backend/.env, which holds the
# backend app's own config (MODEL_API_KEY, etc.) and is loaded via env_file instead.
POSTGRES_USER=tome
POSTGRES_PASSWORD=changeme
POSTGRES_DB=tome
