"""The client plane's data layer: one user's collection and decks.

Lives on the user's own machine and never leaves it. Nothing under
`knowledge_pipeline/` or `knowledge_api/` may import from here.
"""
