fastapi[standard]>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9
httpx>=0.27

# AI / retrieval
# LangChain and ChromaDB were dropped from the stack, not merely unused:
# retrieval is behind an HTTP contract (the Knowledge API) and generation
# behind our own provider interface, and the vector store is pgvector in the
# knowledge Postgres. See CLAUDE.md's stack table.
anthropic>=0.28
sentence-transformers>=3.0

# persistence
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1

# testing
pytest>=8.2
pytest-asyncio>=0.23
